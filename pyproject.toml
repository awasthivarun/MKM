[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mkm-research"
version = "0.1.0"
description = "Research code for electrochemical microkinetic modeling"
requires-python = ">=3.13"

[tool.setuptools]
packages = ["_CO_Oxidation", "_plotting_defaults"]