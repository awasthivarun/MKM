# Raw Data Policy
Files in this directory are treated as raw experimental data.
They should not be modified by preprocessing or modeling code. Any transformed, interpolated, truncated, averaged, or derived datasets should be generated from these files and stored outside `data/raw/`.